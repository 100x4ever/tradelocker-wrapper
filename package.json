{
  "name": "tradelocker-wrapper",
  "version": "1.0.0",
  "description": "TradeLocker charting and automation wrapper for HeroFX",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ws": "^8.16.0"
  }
}
